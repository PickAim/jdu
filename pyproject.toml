[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "jdu"
version = "0.1.15"
dependencies = [
    "SQLAlchemy",
    "requests",
    "aiohttp",
    "jarvis_db @ git+https://github.com/PickAim/jarvis_db.git@v.0.1.19",
]