[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "jdu"
version = "0.1.22"
dependencies = [
    "requests",
    "aiohttp",
    "jorm @ git+https://github.com/PickAim/jorm.git@v.0.1.26",
]